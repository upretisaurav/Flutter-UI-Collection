import 'package:flutter/material.dart';

class MusicTabContent extends StatelessWidget {
  const MusicTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Music Podcasts'),
    );
  }
}
