import 'package:flutter/material.dart';

class NewsPoliticsTabContent extends StatelessWidget {
  const NewsPoliticsTabContent({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('News & Politics Podcasts'),
    );
  }
}
